/* =========================================
   CANON FILTERING
   ========================================= */

.canon-card {
    opacity: 1;
    transform: translateY(0);
    transition:
        opacity 0.3s ease,
        transform 0.3s ease;
}

.canon-card.hidden {
    display: none;
}


/* =========================================
   FILTER BUTTONS
   ========================================= */

#filters {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-bottom: 2rem;
}

#filters button {
    cursor: pointer;
    padding: 0.7rem 1rem;

    background: transparent;
    border: 1px solid rgba(220, 215, 200, 0.18);

    color: rgba(220, 215, 200, 0.65);

    font-family: inherit;
    font-size: 0.75rem;
    letter-spacing: 0.12em;

    transition:
        color 0.25s ease,
        border-color 0.25s ease,
        background 0.25s ease;
}

#filters button:hover {
    color: #e8e1d2;
    border-color: rgba(220, 215, 200, 0.45);
}

#filters button.active {
    color: #e8e1d2;
    border-color: rgba(220, 215, 200, 0.65);
    background: rgba(220, 215, 200, 0.06);
}


/* =========================================
   FILTER STATUS IDENTITIES
   ========================================= */

.canon-card.locked {
    border-color: rgba(210, 210, 195, 0.25);
}

.canon-card.provisional {
    border-color: rgba(190, 170, 90, 0.25);
}

.canon-card.idea {
    border-color: rgba(150, 150, 150, 0.2);
}

.canon-card.rejected {
    border-color: rgba(120, 70, 70, 0.25);
}


/* =========================================
   MOBILE
   ========================================= */

@media (max-width: 600px) {

    #filters {
        gap: 0.5rem;
    }

    #filters button {
        flex: 1 1 auto;
        min-width: 120px;
    }

}